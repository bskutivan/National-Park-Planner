// feed saved address info to map

// put park codes in array
    //loop through array to make a marker on map

// pull park info from map when user clicks

function initMap() {
    var options = {

        zoom: 8,
        center: {lat: 34.052235, lng: -118.243683  }    
    }
    var map = new 
    
    google.maps.Map(document.getElementById('map'), options)

}